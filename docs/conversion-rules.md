# Conversion Rules

Temperature conversions should use the standard Celsius, Fahrenheit, and Kelvin relationships. Kelvin values below absolute zero are invalid and should be rejected instead of producing misleading output.